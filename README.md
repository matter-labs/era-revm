# Revm - zkSync Era translation layer

This crate acts as a bridge between Revm (Rust EVM implementation) and zkSync Era VM.

Currently this is used by Foundry testing. 

This repo is still in development.